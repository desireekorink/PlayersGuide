for (char ch: word.toCharArray()) { if (Character.isUpperCase(ch)) }

if word(input)=< 6 charlength && word(input)=> 13 charlength
 validate = true
else validate = false

for (char c : charArray) {
            if (Character.isUpperCase(c)) {
                hasUpperCase = true;
		}
	     if (c.isDigit())
		hasDigit = true;
        	}
	     if (c == 'T') || (c == '&')
		hasAmpesandOrT = true;
		}
	}
if


if string.matches("T") || string.matches("&")
 validate true
else validate = false
